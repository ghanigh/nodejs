import express from 'express'

const app = express()

// PORT
const PORT = PORT = 8080

// MIDDLEWARE

// MIDDLEWARE TO ROUTE

// LISTEN

app.listen(PORT, () => {
    console.log('Listening at http://localhost:${PORT}');
})